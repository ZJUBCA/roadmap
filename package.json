{
    "scripts": {
        "format:prettier": "prettier --write docs/**/*.md",
        "prepare": "husky"
    },
    "devDependencies": {
        "prettier": "^3.2.5",
        "husky": "^9.0.11",
        "lint-staged": "^15.2.2"
    },
    "lint-staged": {
        "docs/**/*.md": "prettier --write --ignore-unknown"
    }
}